import {it,expect} from 'vitest'
import {UAH_RATES,FX_STATUS,setExchangeRates,subscribeExchangeRates,exchangeRateVersion} from '@/lib/money/fx'
it('publishes rate updates independently of unchanged transaction data',()=>{const before={...UAH_RATES},status={...FX_STATUS};let calls=0;const unsubscribe=subscribeExchangeRates(()=>calls++),version=exchangeRateVersion();try{setExchangeRates({USD:before.USD+1},'2026-09-15');expect(UAH_RATES.USD).toBe(before.USD+1);expect(exchangeRateVersion()).toBe(version+1);expect(calls).toBe(1);setExchangeRates({USD:before.USD+1},'2026-09-15');expect(calls).toBe(1);setExchangeRates({USD:-5,UAH:100},'2026-09-15');expect(UAH_RATES.USD).toBe(before.USD+1);expect(UAH_RATES.UAH).toBe(1)}finally{unsubscribe();setExchangeRates(before,status.date,status.source)}})