{
  "framework": "nextjs",
  "buildCommand": "npm audit --omit=dev --audit-level=high && npm run type-check && NODE_ENV=test npx vitest run tests/unit/ src/__tests__/middleware.test.ts --reporter=dot && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "outputDirectory": ".next"
}